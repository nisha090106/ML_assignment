Assignment 9

What is implemented
- Contains `Assignment_9.ipynb` for the assignment solution.
- Uses `Mall_Customers.csv` for analysis and modeling tasks.
- Demonstrates data exploration, visualization, and machine learning flow.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `Assignment_9.ipynb` using File > Upload notebook.
3. Upload `Mall_Customers.csv` to the Colab session or mount Google Drive.
4. Execute the notebook cells in order.

Implementation notes
- The notebook most likely uses `pandas`, `numpy`, `matplotlib`, `seaborn`, and `scikit-learn`.
- Verify the dataset path before running the data load step.
- Install missing packages in Colab with `!pip install` if needed.
