Assignment 5

What is implemented
- Contains `Assignment_5_.ipynb` that performs the assignment tasks.
- Uses `uber.csv` for data analysis and modeling.
- Covers data preprocessing, visualization, and any machine learning steps required by the assignment.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `Assignment_5_.ipynb` using File > Upload notebook.
3. Upload `uber.csv` to the Colab session or mount Google Drive.
4. Run the notebook cells in order.

Implementation notes
- The notebook likely depends on `pandas`, `numpy`, `matplotlib`, `seaborn`, and possibly `scikit-learn`.
- Confirm that the dataset file path inside the notebook matches the uploaded file location.
- Use `!pip install` to add any missing packages in Colab.
