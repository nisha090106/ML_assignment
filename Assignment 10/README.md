Assignment 10

What is implemented
- Contains `Assignment_10.ipynb` with the assignment work.
- Uses `temperatures.csv` for data analysis and any predictive modeling tasks.
- Includes preprocessing, visualization, and results interpretation.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `Assignment_10.ipynb` using File > Upload notebook.
3. Upload `temperatures.csv` to the Colab session or mount Google Drive.
4. Run all cells sequentially.

Implementation notes
- The notebook likely uses `pandas`, `numpy`, `matplotlib`, and potentially `seaborn`.
- Confirm the dataset file path inside the notebook matches the uploaded file location.
- Install any missing packages with `!pip install` in Colab.
