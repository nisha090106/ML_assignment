Assignment 6

What is implemented
- Contains `assignment_6.ipynb` with the assignment workflow.
- Uses `diabetes.csv` for data analysis and modeling.
- Demonstrates preprocessing, exploratory analysis, and model building.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `assignment_6.ipynb` using File > Upload notebook.
3. Upload `diabetes.csv` to the Colab session or mount Google Drive.
4. Execute cells sequentially.

Implementation notes
- The notebook likely uses `pandas`, `numpy`, `matplotlib`, and `scikit-learn`.
- Ensure the dataset file is in the correct directory before running the data load cells.
- Install any missing libraries in Colab with `!pip install`.
