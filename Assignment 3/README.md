Assignment 3

What is implemented
- Contains `Assignment_3.ipynb` with the assignment solution.
- Includes data exploration, analysis, and model-building steps relevant to the assignment.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `Assignment_3.ipynb` using File > Upload notebook.
3. If the notebook uses external data, upload the required files or mount Google Drive.
4. Execute the notebook cells sequentially.

Implementation notes
- The notebook uses Python data science packages such as `pandas`, `numpy`, `matplotlib`, and possibly `seaborn`.
- Colab usually includes these packages by default, but install missing packages with `!pip install`.
- The notebook is designed for interactive experimentation and result visualization.
