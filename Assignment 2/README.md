Assignment 2

What is implemented
- Contains `Assignment_2.ipynb` for the assignment implementation.
- Uses `train.csv` and `test.csv` for model training and evaluation.
- Demonstrates data preprocessing, training logic, and prediction workflow.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `Assignment_2.ipynb` using File > Upload notebook.
3. Upload `train.csv` and `test.csv` to the Colab session or mount Google Drive.
4. Run the notebook cells in order.

Implementation notes
- The notebook likely uses libraries such as `pandas`, `numpy`, `matplotlib`, and `scikit-learn`.
- Ensure dataset files are available in the runtime environment before running data-loading cells.
- Use `!pip install scikit-learn` in Colab if the package is unavailable.
