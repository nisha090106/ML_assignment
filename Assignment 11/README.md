Assignment 11

What is implemented
- Contains `assignment_11.ipynb` for the assignment solution.
- Uses `Admission_Predict.csv` for data preprocessing, exploration, and modeling.
- Covers prediction and evaluation steps relevant to admission prediction tasks.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `assignment_11.ipynb` using File > Upload notebook.
3. Upload `Admission_Predict.csv` to the Colab session or mount Google Drive.
4. Run the notebook cells sequentially.

Implementation notes
- Likely uses `pandas`, `numpy`, `matplotlib`, `seaborn`, and `scikit-learn`.
- Ensure the dataset file path is correct before executing data-loading cells.
- Use `!pip install` in Colab if packages are missing.
