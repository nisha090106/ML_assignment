Assignment 13

What is implemented
- Contains `Assignment_13.ipynb` with the assignment solution.
- Uses `Iris.csv` for dataset analysis and modeling.
- Includes data exploration, classification modeling, and result visualization.

How to run in Google Colab
1. Open [Google Colab](https://colab.research.google.com).
2. Upload `Assignment_13.ipynb` using File > Upload notebook.
3. Upload `Iris.csv` to the Colab session or mount Google Drive.
4. Run the notebook cells sequentially.

Implementation notes
- The notebook likely uses `pandas`, `numpy`, `matplotlib`, and `scikit-learn`.
- Confirm the dataset file path in the notebook before running.
- Install missing packages with `!pip install` if needed.
